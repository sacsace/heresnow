# Railway — https://docs.railway.com/reference/config-as-code
# Postgres 플러그인으로 DATABASE_URL 연결, Variables에 AUTH_SECRET·AUTH_URL 설정

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
releaseCommand = "npx prisma migrate deploy"
